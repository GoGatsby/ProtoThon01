n=int(input("Enter the Number of rows"))
l=[]
g=[]
for i in range(1,n+1):
    g.clear()
    for j in range(i):
        if j==0:
            g.append(1)
        elif j==i-1:
            g.append(1)
        else:
            g.append(l[j-1]+l[j])
    print(g)
    l=g
    
